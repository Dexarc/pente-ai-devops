{
    "name": "pii-stripper-lambda",
    "version": "1.0.0",
    "description": "Lambda function to strip PII from logs.",
    "main": "index.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
      "aws-sdk": "^2.1093.0"
    }
  }